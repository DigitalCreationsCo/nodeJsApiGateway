This application is an API Gateway built with node.js, express.
It features axios for handling http requests.

This is a fully featured API Gateway that utilizes http forwarding to dynamically access different api endpoints.
The Gateway also features a registry that contains registered services, and utilizes outward facing endpoints,
so that APIs can be registered and unregistered to the Gateway.
